# DLQ Monitor Service - Minimal dependencies
# Essential dependencies only (no Whisper, PyTorch, ffmpeg)

# AWS Services
boto3==1.40.25
botocore==1.40.25

# FastAPI (for configuration loading)
fastapi==0.104.1
pydantic==2.5.0
pydantic-settings==2.1.0

# Environment and configuration
python-dotenv==1.0.0

# HTTP requests (for health checks)
requests==2.32.5

# Logging and utilities
typing_extensions==4.14.1
